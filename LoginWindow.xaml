<Window x:Class="QuizGame1WPF.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#ECEFF1" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quiz Game Login" FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,10,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <Label Content="Username:"/>
            <TextBox x:Name="UsernameBox" Height="30" Margin="0,0,0,10"/>
            <Label Content="Password:"/>
            <PasswordBox x:Name="PasswordBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button Content="Login" Width="100" Margin="10,0" Click="Login_Click"/>
            <Button Content="Exit" Width="100" Click="Exit_Click"/>
        </StackPanel>

        <TextBlock x:Name="ErrorText" Grid.Row="3" Foreground="Red" 
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10"/>
    </Grid>
</Window>
