<Window x:Class="QuizGame1WPF.TeacherDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teacher Dashboard" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quiz Questions" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid x:Name="QuestionsGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                <DataGridTextColumn Header="Question" Binding="{Binding Question}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="100"/>
                <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Add" Width="80" Margin="5" Click="Add_Click"/>
            <Button Content="Edit" Width="80" Margin="5" Click="Edit_Click"/>
            <Button Content="Delete" Width="80" Margin="5" Click="Delete_Click"/>
            <Button Content="View Scores" Width="100" Margin="5" Click="ViewScores_Click"/>
        </StackPanel>
    </Grid>
</Window>
